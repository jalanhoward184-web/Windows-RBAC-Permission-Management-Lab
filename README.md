# Windows-RBAC-Permission-Management-Lab
This lab demonstrates how to implement Role‑Based Access Control (RBAC) in Windows using local users, groups, and NTFS folder permissions. I created two users (Jane and Bob), assigned them to role‑based groups (Accounting and Sales), and configured folder permissions so each user only has access to the resources assigned to their role.
